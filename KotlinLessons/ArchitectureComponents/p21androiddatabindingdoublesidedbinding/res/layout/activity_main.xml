<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <variable
            name="employee"
            type="ru.korobeynikov.p21androiddatabindingdoublesidedbinding.Employee" />

        <variable
            name="mainActivity"
            type="ru.korobeynikov.p21androiddatabindingdoublesidedbinding.MainActivity" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <EditText
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@={employee.name}" />

        <CheckBox
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:checked="@={employee.enabled}"
            android:text="@string/enabled" />

        <Button
            android:id="@+id/button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="@{()->mainActivity.showEmployee()}"
            android:text="@string/showEmployee" />
    </LinearLayout>

</layout>